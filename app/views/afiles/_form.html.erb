<%= form_for(@afile) do |f| %>
  <% if @afile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@afile.errors.count, "error") %> prohibited this afile from being saved:</h2>

      <ul>
      <% @afile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
